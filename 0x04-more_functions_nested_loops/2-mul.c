#include "main.h"

/**
 * main - main function
 * @c:c - input parameter
 * int mul(int a, int b) - function to multiply integers
 *
 * Return: Always return 0.
 */

int mul(int a, int b);
{
	int multiple;

	multiple = a * b;
	return (multiple);
}
