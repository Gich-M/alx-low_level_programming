#include "main.h"

/**
 * main - main function
 * @c:c - input parameter
 * int _isupper(int c) - checks for number
 *
 * Return: Always return 0.
 */

int _isdigit(int c)
{
	if (c >= 49 && c <= 57)
		return (1);
	else
		return (0);
}
