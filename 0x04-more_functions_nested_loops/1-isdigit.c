#include "main.h"

/**
 * _isupper - function that checks for digit.
 * @c: input parameter
 * Return: 1 if c is digit, 0 otherwise
 */

int _isdigit(int c)
{
	if (c >= 49 && c <= 57)
		return (1);
	else
		return (0);
}i
