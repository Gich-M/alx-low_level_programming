#include "main.h"

/**
 * int - input type parameters
 *
 * _islower - function checks for lowercase characters
 *
 * Return: returns 1 if lowercase and 0 if otherwise
 *
 */
int _islower(int)
{
	/*function to check for lowercase characters*/
	int c;

	if (islower(c))
		return (1);
	else
		return (0);
}
